spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
  profiles:
    active: dev  #加载服务ID+"-dev" 即nacos-config-client-dev.yml文件
server:
  port: 8055
